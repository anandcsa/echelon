import {test,expect} from '@playwright/test';
import {newStory} from '../shared/cast.js';
test('witness evidence and trust allow the consent ending at the core',async({page})=>{const story=newStory();story.evidence=['consent','ledger','witness','accord'];for(const n of Object.values(story.cast))n.trust=1;await page.addInitScript(s=>{localStorage.setItem('echelon-v2-save',JSON.stringify({position:[0,1.85,-114],relays:[true,true,true],health:100,campaign:{signal:true,choice:'rescue',story:s}}));localStorage.setItem('echelon-controls-v2',JSON.stringify({profile:'remote'}));},story);await page.goto('/');await page.waitForFunction(()=>window.echelon?.getState().ready,null,{timeout:90000});await page.locator('#continue').click();await page.keyboard.press('e');await expect(page.locator('#dialog-title')).toContainText('WHO GETS TO CHOOSE');await page.getByRole('button',{name:'ACCORD · let every identity choose',exact:true}).click();await expect(page.locator('#ending-title')).toContainText('THE RIGHT TO REFUSE');expect(await page.evaluate(()=>window.echelon.getState().campaign.story.ending)).toBe('accord');await page.screenshot({path:'continuity-accord-ending.png'});});
