import {test} from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import * as T from 'three';import {GLTFLoader} from 'three/addons/loaders/GLTFLoader.js';
test('exported characters have human proportions and usable animation joints',async()=>{for(const id of ['mara','rook','imani','echo']){const b=await readFile(new URL('../public/assets/npc-'+id+'.glb',import.meta.url));const {scene}=await new GLTFLoader().parseAsync(b.buffer.slice(b.byteOffset,b.byteOffset+b.byteLength),'');const size=new T.Box3().setFromObject(scene).getSize(new T.Vector3());assert(size.y>1.7&&size.y<2,`${id}: height ${size.y}`);assert(size.x<.9&&size.z<.6,`${id}: body proportions`);for(const joint of ['Head','Arm_L','Arm_R','Leg_L','Leg_R'])assert(scene.getObjectByName(joint),`${id}: ${joint}`);}});
