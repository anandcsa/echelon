export const EXPANDED_BOUNDS={minX:-235,maxX:235,minZ:-136,maxZ:425};
export const DESTINATIONS=[{id:'kairos',name:'Kairos / Old City',x:0,z:55},{id:'harbor',name:'Glass Harbor',x:-150,z:215},{id:'neon',name:'Neon Exchange',x:150,z:170},{id:'archive',name:'Memory Depot',x:150,z:310},{id:'reservoir',name:'The Reservoir',x:0,z:385}];
export function inWorld(x,z){return Number.isFinite(x)&&Number.isFinite(z)&&x>EXPANDED_BOUNDS.minX&&x<EXPANDED_BOUNDS.maxX&&z>EXPANDED_BOUNDS.minZ&&z<EXPANDED_BOUNDS.maxZ&&(z>=75||Math.abs(x)<68);}
export function newRoam(){return {visited:[],jobs:{courier:'available',blackout:'available',echo:'available'},choices:{},waypoint:null,vehicle:null};}
export function restoreRoam(s){const d=newRoam();if(!s||typeof s!=='object')return d;d.visited=Array.isArray(s.visited)?s.visited.filter(x=>DESTINATIONS.some(p=>p.id===x)):[];for(const k of Object.keys(d.jobs))if(['available','active','complete'].includes(s.jobs?.[k]))d.jobs[k]=s.jobs[k];for(const k of Object.keys(d.jobs))if(typeof s.choices?.[k]==='string')d.choices[k]=s.choices[k].slice(0,60);d.waypoint=DESTINATIONS.some(p=>p.id===s.waypoint)?s.waypoint:null;if(s.vehicle&&inWorld(s.vehicle.x,s.vehicle.z)&&Number.isFinite(s.vehicle.yaw))d.vehicle={x:s.vehicle.x,z:s.vehicle.z,yaw:s.vehicle.yaw};return d;}
export function completeRoam(c,id,choice){if(!Object.hasOwn(c.roam.jobs,id)||c.roam.jobs[id]==='complete')return false;c.roam.jobs[id]='complete';c.roam.choices[id]=choice;c.salvage+=3;const changes=id==='courier'?(choice==='private'?[['rook',1]]:[['mara',1],['rook',-1]]):id==='blackout'?[['imani',1],['mara',1]]:[['echo',1]];for(const [who,delta]of changes)c.story.cast[who].trust=Math.max(-3,Math.min(3,c.story.cast[who].trust+delta));const text=id==='courier'?(choice==='private'?'Rook received Juno’s unedited memory. He refuses to sell it.':'The debt manifest was broadcast. Mara gains evidence; Rook resents the exposure.'):id==='blackout'?'Glass Harbor’s emergency grid is restored. The clinic keeps its witnesses alive.':choice==='free'?'Echo’s missing instance was released as a separate identity.':'Echo’s missing instance entered a protected archive instead of being merged.';c.story.events.push(text);c.story.events=c.story.events.slice(-12);return text;}
