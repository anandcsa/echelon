import {chromium} from '@playwright/test';
const browser=await chromium.launch({channel:'chrome',args:['--no-sandbox','--enable-unsafe-swiftshader']});
try{const page=await browser.newPage({viewport:{width:1100,height:760}});page.setDefaultTimeout(120000);page.on('pageerror',e=>console.log('ERROR',e.message));page.on('response',r=>{if(r.status()>=400)console.log('HTTP',r.status(),r.url())});await page.goto((process.env.REVIEW_URL||'http://127.0.0.1:5190/')+'?quality=balanced');await page.waitForFunction(()=>window.echelon?.getState().ready);await page.screenshot({path:'living-city-title.png'});await page.locator('#start').click();await page.waitForTimeout(2000);await page.screenshot({path:'living-city-game.png'});console.log(JSON.stringify(await page.evaluate(()=>window.echelon.getState())));}finally{await browser.close();}
